<%= simple_form_for [alpaca, booking] do |f| %>
  <%= f.input :start_date, as: :date %>
  <%= f.input :end_date, as: :date %>
  <%= f.button :submit, "Book Alpaca", class: "btn btn-primary w-100"%>
  <%# f.input :comment %>
<% end %>
